<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>SFS Credit Review Tool</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
*{margin:0;padding:0;box-sizing:border-box}
:root{--navy:#1a2332;--navy-light:#243044;--navy-lighter:#2d3a4e;--blue:#2d7ff9;--blue-light:#e8f0fe;--blue-dark:#1a5fd4;--red:#e74c3c;--red-light:#fdeaea;--orange:#f39c12;--orange-light:#fef5e7;--yellow:#f1c40f;--yellow-light:#fef9e7;--green:#27ae60;--green-light:#e8f8f0;--gray-50:#f8f9fa;--gray-100:#f1f3f5;--gray-200:#e9ecef;--gray-300:#dee2e6;--gray-400:#ced4da;--gray-500:#adb5bd;--gray-600:#868e96;--gray-700:#495057;--gray-800:#343a40;--gray-900:#212529;--white:#fff;--shadow:0 1px 3px rgba(0,0,0,.08);--shadow-md:0 4px 12px rgba(0,0,0,.1);--shadow-lg:0 8px 24px rgba(0,0,0,.12);--radius:8px;--radius-sm:6px;--radius-lg:12px}
body{font-family:'Inter',sans-serif;background:var(--gray-50);color:var(--gray-900);display:flex;height:100vh;overflow:hidden;font-size:13px;line-height:1.5}
.sidebar{width:280px;min-width:280px;background:var(--navy);color:var(--white);display:flex;flex-direction:column;height:100vh;overflow-y:auto}
.sidebar::-webkit-scrollbar{width:4px}.sidebar::-webkit-scrollbar-thumb{background:var(--navy-lighter);border-radius:4px}
.sidebar-header{padding:20px 16px 12px;border-bottom:1px solid var(--navy-light)}
.sidebar-header h1{font-size:16px;font-weight:700;display:flex;align-items:center;gap:8px}
.sidebar-header h1 i{color:var(--blue);font-size:18px}
.sidebar-header .subtitle{font-size:10px;color:var(--gray-500);margin-top:2px;letter-spacing:.5px;text-transform:uppercase}
.search-section{padding:12px 16px;border-bottom:1px solid var(--navy-light)}
.search-section label{font-size:10px;text-transform:uppercase;letter-spacing:.5px;color:var(--gray-500);margin-bottom:6px;display:block}
.search-row{display:flex;gap:6px}
.search-row input{flex:1;background:var(--navy-light);border:1px solid var(--navy-lighter);color:var(--white);padding:7px 10px;border-radius:var(--radius-sm);font-size:12px;font-family:inherit}
.search-row input::placeholder{color:var(--gray-600)}
.search-row input:focus{outline:none;border-color:var(--blue)}
.search-row button{background:var(--blue);color:var(--white);border:none;padding:7px 12px;border-radius:var(--radius-sm);cursor:pointer;font-size:12px;font-weight:600}
.search-row button:hover{background:var(--blue-dark)}
.quick-info{padding:12px 16px;border-bottom:1px solid var(--navy-light)}
.quick-info h3{font-size:10px;text-transform:uppercase;letter-spacing:.5px;color:var(--gray-500);margin-bottom:8px}
.info-card{background:var(--navy-light);border-radius:var(--radius-sm);padding:10px 12px}
.info-row{display:flex;justify-content:space-between;align-items:center;padding:3px 0;font-size:11px}
.info-row .label{color:var(--gray-500)}.info-row .value{font-weight:600;color:var(--gray-200)}
.sla-section{padding:12px 16px;border-bottom:1px solid var(--navy-light)}
.sla-section h3{font-size:10px;text-transform:uppercase;letter-spacing:.5px;color:var(--gray-500);margin-bottom:8px}
.sla-card{background:var(--navy-light);border-radius:var(--radius-sm);padding:10px 12px}
.sla-bar{height:4px;background:var(--navy-lighter);border-radius:2px;margin:8px 0 4px;overflow:hidden}
.sla-bar-fill{height:100%;background:var(--orange);border-radius:2px;transition:width .3s}
.sla-text{font-size:10px;color:var(--gray-500);display:flex;justify-content:space-between}
.quick-actions{padding:12px 16px;flex:1}
.quick-actions h3{font-size:10px;text-transform:uppercase;letter-spacing:.5px;color:var(--gray-500);margin-bottom:8px}
.action-btn{width:100%;padding:8px 12px;border:none;border-radius:var(--radius-sm);font-size:12px;font-weight:600;cursor:pointer;margin-bottom:6px;display:flex;align-items:center;gap:8px;font-family:inherit;transition:opacity .15s}
.action-btn:hover{opacity:.9}
.action-btn.approve{background:var(--green);color:var(--white)}
.action-btn.decline{background:var(--red);color:var(--white)}
.action-btn.ssp{background:var(--blue);color:var(--white)}
.action-btn.escalate{background:var(--orange);color:var(--white)}
.main{flex:1;overflow-y:auto;padding:0}
.content-wrapper{max-width:1280px;margin:0 auto;padding:20px 24px 100px}
.section{margin-bottom:24px}
.section-title{font-size:15px;font-weight:700;margin-bottom:12px;display:flex;align-items:center;gap:8px}
.overview-cards{display:grid;grid-template-columns:repeat(auto-fill,minmax(140px,1fr));gap:10px}
.ov-card{background:var(--white);border-radius:var(--radius);padding:12px 14px;box-shadow:var(--shadow);border:1px solid var(--gray-200)}
.ov-card .ov-label{font-size:10px;text-transform:uppercase;letter-spacing:.4px;color:var(--gray-600);margin-bottom:4px}
.ov-card .ov-value{font-size:16px;font-weight:700;color:var(--gray-900)}
.ov-card .ov-sub{font-size:10px;color:var(--gray-500);margin-top:2px}
.badge{display:inline-block;padding:2px 8px;border-radius:10px;font-size:10px;font-weight:700}
.badge-green{background:var(--green-light);color:var(--green)}
.badge-red{background:var(--red-light);color:var(--red)}
.badge-orange{background:var(--orange-light);color:var(--orange)}
.badge-blue{background:var(--blue-light);color:var(--blue)}
.blocker-card{background:var(--white);border-radius:var(--radius);box-shadow:var(--shadow);border:1px solid var(--gray-200);margin-bottom:14px;overflow:hidden}
.blocker-inner{display:flex}
.blocker-border{width:5px;min-height:100%}
.border-red{background:var(--red)}.border-orange{background:var(--orange)}.border-yellow{background:var(--yellow)}
.blocker-content{flex:1;padding:16px 18px}
.blocker-header{display:flex;align-items:center;gap:10px;margin-bottom:4px}
.blocker-header h3{font-size:14px;font-weight:700}
.blocker-tag{font-size:9px;font-weight:600;padding:2px 6px;border-radius:4px;text-transform:uppercase;letter-spacing:.3px}
.tag-critical{background:var(--red-light);color:var(--red)}
.tag-warning{background:var(--orange-light);color:var(--orange)}
.blocker-code{font-size:10px;color:var(--gray-500);font-family:monospace;margin-bottom:8px}
.blocker-desc{font-size:12px;color:var(--gray-700);margin-bottom:10px;padding:8px 12px;background:var(--gray-50);border-radius:var(--radius-sm);border-left:3px solid var(--gray-300)}
.checklist{list-style:none;margin:0;padding:0}
.checklist li{display:flex;align-items:flex-start;gap:8px;padding:6px 0;border-bottom:1px solid var(--gray-100);font-size:12px;color:var(--gray-700)}
.checklist li:last-child{border-bottom:none}
.checklist input[type="checkbox"]{margin-top:2px;accent-color:var(--blue);cursor:pointer;width:15px;height:15px;flex-shrink:0}
.decline-info{margin-top:10px;padding:10px 12px;background:var(--red-light);border-radius:var(--radius-sm);font-size:11px;color:var(--red)}
.decline-info strong{display:block;margin-bottom:2px}
.signal-card{background:var(--white);border-radius:var(--radius);box-shadow:var(--shadow);border:1px solid var(--gray-200);padding:14px 18px;margin-bottom:10px}
.signal-header{display:flex;align-items:center;gap:8px;margin-bottom:6px}
.signal-header h4{font-size:13px;font-weight:600}
.signal-icon{width:28px;height:28px;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:12px;flex-shrink:0}
.signal-icon.warn{background:var(--orange-light);color:var(--orange)}
.signal-icon.ok{background:var(--green-light);color:var(--green)}
.signal-icon.info{background:var(--blue-light);color:var(--blue)}
.signal-detail{font-size:12px;color:var(--gray-600);margin-bottom:4px}
.signal-detail strong{color:var(--gray-800)}
.signal-assessment{font-size:11px;padding:6px 10px;border-radius:var(--radius-sm);margin-top:6px}
.assessment-warn{background:var(--orange-light);color:#b45309}
.assessment-ok{background:var(--green-light);color:#15803d}
.passed-section{background:var(--white);border-radius:var(--radius);box-shadow:var(--shadow);border:1px solid var(--gray-200);overflow:hidden}
.collapsible-header{padding:14px 18px;cursor:pointer;display:flex;align-items:center;justify-content:space-between;user-select:none}
.collapsible-header:hover{background:var(--gray-50)}
.collapsible-header h3{font-size:14px;font-weight:600;display:flex;align-items:center;gap:8px}
.passed-item{display:flex;align-items:center;gap:10px;padding:6px 0;font-size:12px;border-bottom:1px solid var(--gray-100)}
.passed-item:last-child{border-bottom:none}
.passed-item i{color:var(--green);font-size:14px}
.decision-section{background:var(--white);border-radius:var(--radius-lg);box-shadow:var(--shadow-md);border:1px solid var(--gray-200);padding:20px 24px}
.progress-bar{height:8px;background:var(--gray-200);border-radius:4px;overflow:hidden}
.progress-fill{height:100%;background:linear-gradient(90deg,var(--blue),var(--green));border-radius:4px;transition:width .3s}
.decision-buttons{display:flex;gap:10px;flex-wrap:wrap;margin-top:16px}
.decision-btn{padding:10px 20px;border:none;border-radius:var(--radius);font-size:13px;font-weight:600;cursor:pointer;display:flex;align-items:center;gap:6px;font-family:inherit;transition:all .15s}
.decision-btn:hover{transform:translateY(-1px);box-shadow:var(--shadow-md)}
.decision-btn.d-approve{background:var(--green);color:var(--white)}
.decision-btn.d-decline{background:var(--red);color:var(--white)}
.decision-btn.d-ssp{background:var(--blue);color:var(--white)}
.decision-btn.d-escalate{background:var(--orange);color:var(--white)}
.empty-state{display:flex;flex-direction:column;align-items:center;justify-content:center;height:100vh;color:var(--gray-500);text-align:center;padding:40px}
.empty-state i{font-size:48px;margin-bottom:16px;color:var(--gray-300)}
.empty-state h2{font-size:18px;color:var(--gray-600);margin-bottom:8px}
.empty-state p{font-size:13px;max-width:300px}
.toast{position:fixed;bottom:24px;right:24px;background:var(--gray-900);color:var(--white);padding:12px 20px;border-radius:var(--radius);font-size:13px;font-weight:500;box-shadow:var(--shadow-lg);z-index:2000;transform:translateY(80px);opacity:0;transition:all .3s}
.toast.show{transform:translateY(0);opacity:1}
.toast i{margin-right:6px}
.loading{display:flex;flex-direction:column;align-items:center;justify-content:center;height:100vh;color:var(--gray-500)}
.spinner{width:40px;height:40px;border:4px solid var(--gray-200);border-top:4px solid var(--blue);border-radius:50%;animation:spin 1s linear infinite;margin-bottom:16px}
@keyframes spin{to{transform:rotate(360deg)}}
.biz-link{color:var(--gray-400);font-size:11px;text-decoration:none;transition:color .15s;display:inline-flex;align-items:center;margin-left:6px}
.biz-link:hover{color:var(--blue)}
</style>
</head>
<body>

<aside class="sidebar">
  <div class="sidebar-header">
    <h1><i class="fas fa-shield-halved"></i> Credit Review</h1>
    <div class="subtitle">SFS Pre-Origination Tool</div>
  </div>
  <div class="search-section">
    <label>Application Token</label>
    <div class="search-row">
      <input type="text" id="tokenInput" placeholder="A-XXXXXXXXXX">
      <button onclick="lookupApplication()"><i class="fas fa-search"></i></button>
    </div>
  </div>
  <div id="sidebarInfo" style="display:none">
    <div class="quick-info">
      <h3>Merchant Info</h3>
      <div class="info-card" id="merchantInfoCard"></div>
    </div>
    <div class="sla-section">
      <h3>SLA Tracker</h3>
      <div class="sla-card" id="slaCard"></div>
    </div>
    <div class="quick-actions">
      <h3>Quick Actions</h3>
      <button class="action-btn approve" onclick="showToast('Approve flow coming soon')"><i class="fas fa-check"></i> Approve</button>
      <button class="action-btn decline" onclick="showToast('Decline flow coming soon')"><i class="fas fa-times"></i> Decline</button>
      <button class="action-btn ssp" onclick="openSSPModal()"><i class="fas fa-envelope"></i> Send SSP</button>
      <button class="action-btn escalate" onclick="openEscModal()"><i class="fas fa-arrow-up"></i> Escalate</button>
    </div>
  </div>
</aside>

<main class="main">
  <div id="emptyState" class="empty-state">
    <i class="fas fa-shield-halved"></i>
    <h2>SFS Credit Review Tool</h2>
    <p>Enter an application token to load the review dashboard with real data from Snowflake.</p>
  </div>
  <div id="loadingState" class="loading" style="display:none">
    <div class="spinner"></div>
    <p>Loading application data from Snowflake...</p>
  </div>
  <div id="errorState" style="display:none;padding:40px;text-align:center;color:var(--red)">
    <i class="fas fa-exclamation-triangle" style="font-size:48px;margin-bottom:16px;display:block"></i>
    <h2 id="errorTitle" style="margin-bottom:8px">Application Not Found</h2>
    <p id="errorMsg" style="color:var(--gray-600)"></p>
  </div>
  <div id="appContent" class="content-wrapper" style="display:none"></div>
</main>

<div class="toast" id="toast"></div>

<!-- Escalation Modal -->
<div class="modal-overlay" id="escModal" style="display:none;position:fixed;top:0;left:0;right:0;bottom:0;background:rgba(0,0,0,.5);z-index:1000;align-items:center;justify-content:center">
<div style="background:var(--white);border-radius:var(--radius-lg);box-shadow:var(--shadow-lg);padding:24px;max-width:550px;width:95%;max-height:85vh;overflow-y:auto">
  <h2 style="font-size:16px;margin-bottom:4px;display:flex;align-items:center;gap:8px"><i class="fas fa-arrow-up" style="color:var(--orange)"></i> Escalate Case</h2>
  <p style="font-size:11px;color:var(--gray-500);margin-bottom:16px">Select the team to escalate to. A pre-filled message will be generated.</p>
  
  <div id="escChannelList" style="display:flex;flex-direction:column;gap:6px;margin-bottom:14px"></div>
  
  <div id="escPreview" style="display:none;background:var(--gray-50);border:1px solid var(--gray-200);border-radius:var(--radius-sm);padding:14px;margin-bottom:12px">
    <div style="font-size:10px;text-transform:uppercase;letter-spacing:.5px;color:var(--gray-500);margin-bottom:4px">Escalation Message</div>
    <div id="escMessage" style="font-size:12px;color:var(--gray-700);white-space:pre-wrap;line-height:1.6;font-family:monospace;background:var(--white);padding:10px;border-radius:var(--radius-sm);border:1px solid var(--gray-200)"></div>
  </div>
  
  <textarea id="escNotes" placeholder="Add additional context for the escalation..." style="width:100%;padding:8px 10px;border:1px solid var(--gray-300);border-radius:var(--radius-sm);font-family:inherit;font-size:12px;height:60px;resize:vertical;margin-bottom:12px"></textarea>
  
  <div style="display:flex;gap:8px;justify-content:flex-end">
    <button onclick="closeEscModal()" style="padding:8px 18px;border:none;border-radius:var(--radius-sm);font-size:12px;font-weight:600;cursor:pointer;background:var(--gray-200);color:var(--gray-700);font-family:inherit">Cancel</button>
    <button onclick="copyEscMessage()" style="padding:8px 18px;border:none;border-radius:var(--radius-sm);font-size:12px;font-weight:600;cursor:pointer;background:var(--gray-800);color:var(--white);font-family:inherit"><i class="fas fa-copy"></i> Copy Message</button>
    <button onclick="sendEscalation()" style="padding:8px 18px;border:none;border-radius:var(--radius-sm);font-size:12px;font-weight:600;cursor:pointer;background:var(--orange);color:var(--white);font-family:inherit"><i class="fas fa-paper-plane"></i> Escalate</button>
  </div>
</div>
</div>

<!-- SSP Modal -->
<div class="modal-overlay" id="sspModal" style="display:none;position:fixed;top:0;left:0;right:0;bottom:0;background:rgba(0,0,0,.5);z-index:1000;align-items:center;justify-content:center">
<div style="background:var(--white);border-radius:var(--radius-lg);box-shadow:var(--shadow-lg);padding:24px;max-width:700px;width:95%;max-height:85vh;overflow-y:auto">
  <h2 style="font-size:16px;margin-bottom:4px;display:flex;align-items:center;gap:8px"><i class="fas fa-envelope" style="color:var(--blue)"></i> Send SSP Request</h2>
  <p style="font-size:11px;color:var(--gray-500);margin-bottom:16px">Select a template category, then choose the specific SSP to send.</p>
  
  <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap" id="sspCategoryBtns"></div>
  
  <select id="sspTemplateSelect" style="width:100%;padding:8px 10px;border:1px solid var(--gray-300);border-radius:var(--radius-sm);font-family:inherit;font-size:12px;margin-bottom:12px" onchange="showSSPTemplate()">
    <option value="">— Select an SSP template —</option>
  </select>
  
  <div id="sspPreview" style="display:none;background:var(--gray-50);border:1px solid var(--gray-200);border-radius:var(--radius-sm);padding:14px;margin-bottom:12px">
    <div style="font-size:10px;text-transform:uppercase;letter-spacing:.5px;color:var(--gray-500);margin-bottom:4px">Type</div>
    <div id="sspType" style="font-size:12px;font-weight:600;margin-bottom:8px"></div>
    <div style="font-size:10px;text-transform:uppercase;letter-spacing:.5px;color:var(--gray-500);margin-bottom:4px">Template Text</div>
    <div id="sspText" style="font-size:12px;color:var(--gray-700);white-space:pre-wrap;line-height:1.6"></div>
    <div id="sspUse" style="margin-top:10px;padding:8px 10px;background:var(--blue-light);border-radius:var(--radius-sm);font-size:11px;color:var(--blue-dark)"></div>
  </div>
  
  <textarea id="sspNotes" placeholder="Add notes or customize the template..." style="width:100%;padding:8px 10px;border:1px solid var(--gray-300);border-radius:var(--radius-sm);font-family:inherit;font-size:12px;height:60px;resize:vertical;margin-bottom:12px"></textarea>
  
  <div style="display:flex;gap:8px;justify-content:flex-end">
    <button onclick="closeSSPModal()" style="padding:8px 18px;border:none;border-radius:var(--radius-sm);font-size:12px;font-weight:600;cursor:pointer;background:var(--gray-200);color:var(--gray-700);font-family:inherit">Cancel</button>
    <button onclick="copySSPText()" style="padding:8px 18px;border:none;border-radius:var(--radius-sm);font-size:12px;font-weight:600;cursor:pointer;background:var(--gray-800);color:var(--white);font-family:inherit"><i class="fas fa-copy"></i> Copy Text</button>
    <button onclick="sendSSP()" style="padding:8px 18px;border:none;border-radius:var(--radius-sm);font-size:12px;font-weight:600;cursor:pointer;background:var(--blue);color:var(--white);font-family:inherit"><i class="fas fa-paper-plane"></i> Send SSP</button>
  </div>
</div>
</div>

<script>
// ============ SSP TEMPLATES ============
const SSP_TEMPLATES = {
  "Processing": [
    {name:"Seasonality - Repayment (TEXT)",type:"TEXT",text:"You're required to pay at least 1/18 of your initial loan balance every 60 days so that you make consistent repayment progress. Do you wish to continue with your Square Loan application? (Yes/No)",use:"Reminding merchant of their minimum payment obligation. Often sent for sporadic processing (events-based or seasonal merchants)."},
    {name:"Seasonality - Inquiry (TEXT)",type:"TEXT",text:"Please explain the seasonality of your business.",use:"Understand if the merchant is seasonal, and where we are catching them in their busy season."},
    {name:"Processing Gap (TEXT)",type:"TEXT",text:"Please explain the processing gap between [Start_DATE] and [End_DATE].",use:"Understand why the merchant stopped processing."},
    {name:"Linked Account Slow Down (TEXT)",type:"TEXT",text:"Please provide an explanation for the decline in processing with an associated account under the email address: (email_address). When do you anticipate this account returning to normal processing?",use:"Understand the slow down in processing on a linked account."},
    {name:"Same Business Inquiry (TEXT)",type:"TEXT",text:"Please explain the difference between this Square account and the Square account under the email address (email_address). Are the two Square accounts operating under the same legal entity?",use:"Understand why the merchant has multiple accounts. Are they opening new accounts after receiving a Square Loan to get another?"},
    {name:"Same Business Inquiry (DOC)",type:"DOC",text:"Please provide business documentation for your linked Square account under the email address: (email_address).",use:"Understand why the merchant has multiple accounts with documentation."},
    {name:"Payment Volume Change (TEXT)",type:"TEXT",text:"Please explain the recent processing change with Square. If the change in processing is temporary, when do you expect to return to your normal processing?",use:"Understand any recent changes to the business. Is the business slowing down, moving or closing?"},
    {name:"Outlier Transactions - Invoice/Contract (DOC)",type:"DOC",text:"Please supply an invoice, service contract, or itemized document for the goods or services provided to the customer using the [card_type] card ending in [last_4_digits]. This request is in relation to the transaction in the amount of $[dollar_amount] on [date]. For more information about acceptable documentation, visit our Help Center Article.",use:"Confirm that outlier transactions are not cash advancing and provide additional context for approval."},
    {name:"Outlier Transactions - Relationship (TEXT)",type:"TEXT",text:"Please explain your business relationship with the customer paying with the [card_type] card ending in [last_4_digits].",use:"Understand what the recent spike in payments is related to."},
    {name:"Bank Statements (3x DOCS)",type:"DOC",text:"Please provide your three most recent bank statements for the bank account linked to Square ending in [last_4_digits].",use:"For ACH Debit declines where the Merchant provides Bank Statements to verify causes of Failed Debits."},
    {name:"Payment Slowdown (TEXT)",type:"TEXT",text:"Please explain why your business is processing fewer transactions through Square. When do you expect to resume normal processing?",use:"Understand the specific reason for the slow down in processing."},
    {name:"Chargeback Inquiry (TEXT)",type:"TEXT",text:"Please explain the recent chargeback activity against your business.",use:"Understand the specific reason for an increase in chargebacks."},
    {name:"Refund Inquiry (TEXT)",type:"TEXT",text:"Please provide an explanation for the increase in refunds on your Square processing account beginning around [date].",use:"Understand the specific reason for an increase in refunds."}
  ],
  "Debt & Liens": [
    {name:"Outstanding UCC Lien - Debt Overview (TEXT)",type:"TEXT",text:"Please provide an overview of your business's debt.",use:"When the merchant is a sole prop and does not appear on a UCC lien search or has multiple liens represented by Corporation Services Company."},
    {name:"UCC Termination (DOC)",type:"DOC",text:"Please provide a filed UCC termination statement (UCC-3) for a UCC financing statement (UCC-1) filed on [filing_date] (filing number [filing_number]). The secured party is listed as [secured_party]. For more information regarding UCC liens, please visit our Help Center Article.",use:"Documentation showing that a secured financial obligation has been paid off."},
    {name:"Additional Debt Allowance (DOC)",type:"DOC",text:"A UCC-1 financing statement filed against your business indicates that [secured_party] has a security interest in your business' assets. To continue with the application process, please provide a filed UCC termination statement (UCC-3), or signed documentation from [secured_party] confirming that your business is in good standing and authorized to establish a lender relationship elsewhere.",use:"Verifies that a Merchant is not violating current obligations by taking on additional debt."},
    {name:"Placement Lien (DOC)",type:"DOC",text:"Square Financial Services has identified through a public search that a lien has been filed against you or your business. Please provide documentation showing that you are current on the obligations associated with the following lien(s):\n\n$[lien_amount] filed on [filing_date] by [secured_party]\n\nDocumentation can include: (1) the most recent payment statement showing that you are current on the obligation, or (2) documentation evidencing release of the lien.",use:"Request for documentation showing the loan applicant is in good standing with existing creditors (typically IRS or State)."},
    {name:"Judgment (DOC)",type:"DOC",text:"Please provide documentation verifying that the judgment entered against you and/or your business on [filing_date] in the amount $[judgement_amount] has been satisfied, vacated or otherwise resolved.",use:"Verifies that the judgment has been paid, set aside or otherwise resolved."},
    {name:"Bankruptcy (DOC)",type:"DOC",text:"Please provide documentation that the bankruptcy case filed by you and/or your business on [filing_date] has been closed, dismissed or otherwise resolved.",use:"Verifies outcome of bankruptcy and any remaining obligation to creditors."},
    {name:"Payment Confirmation (DOC)",type:"DOC",text:"Please provide documentation showing that you made your payment due on [date] to [secured_party].",use:"Confirm that a merchant is current on repayment with outside debt/payment plan."},
    {name:"UCC Lien (DOC)",type:"DOC",text:"Square Financial Services has identified through a public search that your business is subject to one or more outstanding UCC financing statements (UCC-1). Please provide documentation showing that you are current on the obligations associated with the following UCC filing:\n\n[filing_date] (filing number [filing_number]) The secured party is [secured_party].\n\nDocumentation can include: (1) the most recent payment statement showing that you are current on the obligation, or (2) a filed UCC termination statement (UCC-3).\n\nIf this loan is in forbearance or you are on a payment plan, please provide (1) documentation of your most recent payment, and (2) the entire signed forbearance agreement or payment plan.",use:"Request documentation for outstanding UCC liens. #1 reason sellers call into the SSP inbound line."}
  ],
  "Business Legitimacy": [
    {name:"Business Government Documentation (DOC)",type:"DOC",text:"Please provide a government-issued document about your business, such as a business license, annual report, seller's license, tax return, or articles of incorporation.",use:"When unsure if the business exists and there is no web presence."},
    {name:"Recent Bank Account Change (DOC)",type:"DOC",text:"Please verify your recent bank account change on your Square account by uploading your most recent bank statement, or a screenshot of your most recent bank statement, showing your name, last three digits of your account number, and current bank transactions for the account ending in [last_3_digits].",use:"Verifies legitimacy of bank change to avoid risk of ATO."},
    {name:"Web Presence (TEXT)",type:"TEXT",text:"Please explain the nature of your business, detailing the types of goods and/or services processed on Square, and provide any links to your online presence if available.",use:"When Credit Analysts are unable to determine the Merchant's business services or products."},
    {name:"Business Inactive with SOS (DOC)",type:"DOC",text:"Your business, [sos_business_name], is currently listed as inactive by the [application_state] Secretary of State. Please provide a reinstatement letter or certificate of good standing from the last 90 days showing that your business is currently active and in good standing.",use:"The business must be active with the Secretary of State to receive Capital."},
    {name:"Entity Verification (TEXT)",type:"TEXT",text:"Your loan application lists your business name as [app_business_name] and the entity type as a [app_business_entity], however your business is listed by the Secretary of State as [sos_business_name], a [sos_entity_type]. Please provide (1) your legal business name, (2) your legal entity type, and (3) the Tax ID number you use for your business.",use:"When a Merchant applies using the wrong entity type."},
    {name:"Entity Verification (DOC)",type:"DOC",text:"Please provide your most recent business tax return or your EIN Confirmation Letter from the IRS (Form SS-4) confirming your TIN/EIN and legal business name. If you have multiple businesses please make sure that the documentation you provide is only for the loan applicant.",use:"Confirm correct business EIN & entity type with documentation."},
    {name:"Beneficial Owners (TEXT)",type:"TEXT",text:"We are required by law to collect information about individuals with direct or indirect ownership in a business that's applying for a loan. Does anyone besides the loan applicant directly or indirectly own 25% or more of the business applying for the loan? If so, for each person, please list his or her first and last name, date of birth, Social Security number, and home address. If no individuals other than the loan applicant directly or indirectly own 25% or more of your business, please reply \"none.\"",use:"Collect beneficial owner information as required by law."},
    {name:"Articles of Incorporation (DOC)",type:"DOC",text:"Please provide a copy of your business's Articles of Incorporation. If your business is a subsidiary of another entity, please also include the parent organization's Articles of Incorporation or organizational documents.",use:"Verifies the entity structure if there are multiple linked accounts."},
    {name:"Loan Authorization (TEXT)",type:"TEXT",text:"Please confirm that the loan applicant is authorized to take out a loan on behalf of the business.",use:"Ensures the loan applicant is authorized — used for name mismatches, different applicants, or suspected business sales."},
    {name:"Business Closed (DOC)",type:"DOC",text:"Your business appears closed based on an internet search. Please provide documentation confirming that your business is open and active with the Secretary of State where your business is registered.",use:"If a business appears closed via public search. Only sent to business entities (not sole props)."},
    {name:"No Linked Bank Account (DOC)",type:"DOC",text:"To proceed with your application, please link a bank account to your Square dashboard. Once completed, please upload a copy of your most recent bank statement, or screenshot(s) showing your name, last three digits of your account number, and current bank transactions for the newly linked bank account.",use:"Merchant has no linked bank account."}
  ],
  "Individual Verification": [
    {name:"Applicant Name (TEXT)",type:"TEXT",text:"We're unable to verify the applicant for this loan request. In order to process your request, please provide your first and last name. By submitting this information, you direct Square Financial Services to update the application on your behalf to match the name provided.",use:"Unable to verify applicant identity."},
    {name:"Bank Account Verification (DOC)",type:"DOC",text:"In order to proceed with your loan application, we need to verify the ownership of your bank account. Please provide your most recent bank statement or a screenshot showing the business name or the Square account owner name, and the last three digits of your account for the account ending in [last_3_digits].",use:"Verify bank account ownership."},
    {name:"Documentation Mismatch (TEXT)",type:"TEXT",text:"The name provided on the application does not match the name listed on your documentation. Please provide an explanation for the name mismatch.",use:"Name on application doesn't match documentation."},
    {name:"DOB Fail (DOC)",type:"DOC",text:"We're unable to verify your date of birth. Please submit a copy of your government-issued ID (i.e. driver's license, passport, etc.). By providing your government-issued ID, you direct Square Financial Services to update the application on your behalf.",use:"Unable to verify date of birth."},
    {name:"SSN Fail 1 (DOC)",type:"DOC",text:"We're unable to verify your Social Security number. Please submit a copy of your Social Security card or Individual Taxpayer Identification Number (ITIN) letter. If you do not have either of these documents, you may provide a copy of your personal tax return.",use:"Unable to verify SSN — first attempt."},
    {name:"SSN Fail 2 (DOC)",type:"DOC",text:"We're unable to verify your Social Security number with the documentation provided. At this time, we're only able to accept a copy of your Social Security card or Individual Taxpayer Identification Number (ITIN) letter to verify your information. If you need help getting a copy of your card please visit: https://www.ssa.gov/ssnumber",use:"Unable to verify SSN — second attempt, stricter requirements."},
    {name:"Deleted Account (TEXT)",type:"TEXT",text:"We see that you have opted to keep this location as inactive with Square. Please provide an explanation.",use:"Merchants sometimes apply for loan but delete their accounts."}
  ],
  "Business Verification": [
    {name:"Business Name Confirmation (TEXT)",type:"TEXT",text:"Your loan application lists your business name as [name on application], however your business is listed as [business name on documents] on the provided documentation. Please confirm the legal business name and Tax Identification Number (TIN) if you would like to move forward with this application.",use:"Discrepancy in business name between application and documentation."},
    {name:"TIN Fail 1 (DOC)",type:"DOC",text:"We're unable to verify your Taxpayer Identification Number (TIN). Please provide your most recent business tax return or your EIN Confirmation Letter from the IRS (Form SS-4) confirming your TIN/EIN and legal business name.",use:"Unable to verify TIN — first attempt."},
    {name:"TIN Fail 2 (DOC)",type:"DOC",text:"We're unable to verify your TIN/EIN with the documentation provided. At this time, we're only able to accept a copy of your IRS Form SS-4 to verify your information.",use:"Unable to verify TIN — second attempt, stricter requirements."},
    {name:"Entity Type (TEXT)",type:"TEXT",text:"Please indicate your business entity type (i.e. sole proprietorship, S Corporation, LLC, etc.) in the field below. By providing your response, you direct Square Financial Services to update the application on your behalf.",use:"Need to confirm entity type."},
    {name:"Business Address Confirmation (DOC)",type:"DOC",text:"We were unable to verify your business address, please supply a document confirming the full address (e.g., utility bill, lease agreement, bank statement, etc.) for your business. Please note, we are unable to accept P.O. box addresses.",use:"Unable to verify business address."}
  ],
  "Name Mismatch": [
    {name:"Name Mismatch 1 (TEXT)",type:"TEXT",text:"The name on the application does not match the name originally used to open the Square account. Please provide an explanation for the name mismatch.",use:"Name on application doesn't match Square account name."},
    {name:"Name Mismatch 2 (DOC)",type:"DOC",text:"Please provide official documentation confirming that the applicant is authorized to apply for this loan on behalf of the business. This can be in the form of Business Tax Returns listing the registered agent, Articles of Incorporation, Articles of Organization, Partnership Agreement, Operating Agreement, or a state or federal business document.\n\nDoes anyone besides the loan applicant directly or indirectly own 25% or more of the business? If so, please list their first and last name, date of birth, Social Security number, and home address.",use:"Need documentation to confirm authorization and beneficial owners."},
    {name:"Name Mismatch Final (DOC)",type:"DOC",text:"The name on the documentation provided does not match that of the loan applicant. Please provide your government-issued photo ID, Social Security card, or passport proving your legal name is [NAME of applicant]. If [NAME of applicant] is not the applicant, please login to your Square Dashboard to cancel the current application and reapply using the correct information.",use:"Final attempt — if name still doesn't match, merchant must cancel and reapply."}
  ],
  "Beneficial Owners": [
    {name:"Beneficial Owner SSN (DOC)",type:"DOC",text:"We're unable to verify the Social Security number (SSN) of [NAME OF Beneficial Owner]. Please submit a copy of their Social Security card or Individual Taxpayer Identification Number (ITIN) letter.",use:"Unable to verify beneficial owner SSN."},
    {name:"Beneficial Owner DOB (DOC)",type:"DOC",text:"We're unable to verify the date of birth submitted for [NAME OF Beneficial Owner] during the Business Verification step. Please provide a copy of their government-issued photo ID (i.e. driver's license, passport, etc.)",use:"Unable to verify beneficial owner date of birth."},
    {name:"Beneficial Owner Passport (DOC)",type:"DOC",text:"We're unable to verify the Passport number for [NAME OF Beneficial Owner]. Please submit a copy of their government-issued passport.",use:"Unable to verify beneficial owner passport."},
    {name:"Post Origination BO Update (TEXT)",type:"TEXT",text:"Please provide the first and last name, home address, date of birth, SSN/ITIN or passport number, and role of the new beneficial owner(s) of the business. If any of this information has changed for any existing beneficial owners, please also provide us with the updated information.",use:"Post-origination beneficial owner update."}
  ],
  "Single Member LLC": [
    {name:"Permission to Update TIN (TEXT)",type:"TEXT",text:"Your Schedule C provided doesn't include the Taxpayer Identification Number (TIN) used in your application. If your business is an LLC operating with your Social Security number (SSN), and you would like us to change the TIN in your application to your SSN, please select \"yes.\" If you have an Employee Identification Number EIN, please select \"no.\"",use:"When merchant provided a number that is neither their EIN or SSN."},
    {name:"Single Member Confirmation (TEXT)",type:"TEXT",text:"Your Schedule C information doesn't match what was provided in the application. If you would like us to update your application, please (1) specify your business entity type, (2) provide your legal business name, and (3) provide the Taxpayer Identification Number.",use:"Confirm single member LLC status and correct business information."},
    {name:"Business Info for SOS Search (TEXT)",type:"TEXT",text:"We can't confirm that your business is a Limited Liability Company (LLC) with the Schedule C you provided. If your business is an LLC, please provide your legal business name and the Taxpayer Identification Number. If you are a sole proprietor, please state that below and provide your \"Doing Business As\" name, if you have one.",use:"Unable to locate merchant's business on Secretary of State."},
    {name:"LLC Operating Agreement (DOC)",type:"DOC",text:"Please provide a copy of your Limited Liability Company (LLC) Operating Agreement in order to confirm that your business is an LLC.",use:"Confirm LLC status with operating agreement."},
    {name:"P.O. Box (TEXT)",type:"TEXT",text:"We are unable to support a P.O. Box address. Please respond with a physical home and business address. By responding to this request, you direct Square Financial Services to update the application on your behalf to match the addresses provided.",use:"Merchant has a P.O. Box address which is not supported."}
  ]
};

let currentSSPCategory = null;

const API_BASE = 'http://localhost:5050/api';

// CROP blocker definitions from the SOP
const CROP_BLOCKERS = {
  plan_amount_too_large: {
    title: 'Plan Amount Too Large',
    severity: 'critical',
    sop: '§7.2 / §7.11',
    desc: 'All loans above $40K are sent to manual review. Loans >$75K require LexisNexis + UCC lien search.',
    checklist: [
      'Review processing history for outlier transactions',
      'Check for seasonality concerns (§7.4)',
      'If >$75K: Conduct LexisNexis public record search (go/sfsln)',
      'If >$75K: Conduct UCC lien search via Lien Solutions (go/ls)',
      'If >$75K: Conduct SOS Search for corporations/LLCs/LPs',
      'If >$250K: Additional bankruptcy search via PACER + max 2 all-asset liens'
    ],
    aan: null
  },
  has_recent_sketchy_login: {
    title: 'Recent Sketchy Login',
    severity: 'warning',
    sop: '§7.9',
    desc: 'The seller has a recent sketchy login flagged on their account.',
    checklist: [
      'Check if seller has 2FA set up on account via go/id',
      'Verify 2FA phone number matches activation number on account',
      'If 2FA set up & phone matches → continue review',
      'If no 2FA or phone mismatch → create ATO alert for ATO team',
      'If ATO unresolved in 7 calendar days → decline 10 days (Business Verification AAN)'
    ],
    aan: {name: 'Business Verification', days: '10 days'}
  },
  chargeback_amount_too_large_last_year: {
    title: 'Chargeback Amount Too Large (12 months)',
    severity: 'critical',
    sop: '§7.4',
    desc: 'Chargeback amount in the past year is larger than 3% of net sales.',
    checklist: [
      'Verify chargeback GPV > 3% total GPV in last 12 months',
      'Check if chargebacks are from same customer',
      'Check for associated failed debits from chargebacks',
      'If CB GPV > 3% → decline 180 days',
      'If failed debits from CB with insufficient funds → decline 180 days'
    ],
    aan: {name: 'Chargeback History', days: '180 days'}
  },
  chargeback_amount_too_large_last_twelve_weeks: {
    title: 'Chargeback Amount Too Large (12 weeks)',
    severity: 'critical',
    sop: '§7.4',
    desc: 'Chargeback amount in the past 12 weeks is larger than 3% of net sales.',
    checklist: [
      'Verify chargeback amount vs 12-week net sales',
      'If business is <50% CP with CB increase in last 45 days and CB GPV ≥ 2% → decline 90 days',
      'Check for associated failed debits'
    ],
    aan: {name: 'Risk of Chargebacks', days: '90 days'}
  },
  chargeback_count_too_large_last_four_weeks: {
    title: 'Too Many Chargebacks (4 weeks)',
    severity: 'critical',
    sop: '§7.4',
    desc: 'More than 4 disputes in the last four weeks.',
    checklist: ['Verify dispute count in last 4 weeks exceeds 4', 'Review dispute reasons and patterns'],
    aan: {name: 'Chargeback History', days: '180 days'}
  },
  frozen: {
    title: 'Account Currently Frozen',
    severity: 'critical',
    sop: '§7.6',
    desc: 'The merchant account is currently frozen.',
    checklist: [
      'Determine reason for freeze (Compliance vs Credit Risk vs Fraud)',
      'If Compliance: escalate to #compliance',
      'If Credit Risk: assess chargeback/risk signals per §7.4',
      'If warned or deactivated → decline',
      'If unfrozen with no warning → continue review'
    ],
    aan: {name: 'Depends on freeze reason', days: '90 days to Permanent'}
  },
  frozen_recently: {
    title: 'Recently Frozen',
    severity: 'warning',
    sop: '§7.6',
    desc: 'The merchant has been frozen in the past 90 days.',
    checklist: ['Review comments to determine reason for freeze', 'Follow same procedure as current freeze'],
    aan: {name: 'Depends on freeze reason', days: '90-180 days'}
  },
  plan_past_due: {
    title: 'Plan Past Due',
    severity: 'critical',
    sop: '§7.10',
    desc: 'Open plan is in a past due state.',
    checklist: ['Verify seller is past due on existing loan', 'Past due sellers do not qualify for new offers → decline'],
    aan: {name: 'Plan Past Due', days: '90 days'}
  },
  connected_users_deactivated_frozen: {
    title: 'Connected Users Frozen/Deactivated',
    severity: 'warning',
    sop: '§7.3',
    desc: 'Hard-linked units are frozen or deactivated.',
    checklist: [
      'Review connected user graph / linked account brick',
      'Determine source of freeze/deactivation on linked account',
      'If due to Compliance/Risk/Legal and poses elevated risk → decline permanently',
      'If Tax-B Notice freeze → continue review',
      'Check for negative balance on linked accounts (>$500 in last 5 years)'
    ],
    aan: {name: 'Adverse Account Activity', days: 'Permanent'}
  },
  connected_users_have_open_plan: {
    title: 'Connected Users Have Open Plan',
    severity: 'warning',
    sop: '§7.3.1',
    desc: 'A hard-linked account has an open Capital plan.',
    checklist: [
      'Review for possible evasion (processing shifted to new account)',
      'Check if linked unit GPV is materially decreasing',
      'Check if processing was split between accounts post-approval',
      'If evasion: 1st offense=90d, 2nd=180d, 3rd=365d, 4th=permanent'
    ],
    aan: {name: 'Linked Account Activity', days: '90-365 days or Permanent'}
  },
  customer_is_blacklisted: {
    title: 'Customer Blacklisted',
    severity: 'critical',
    sop: '§6.1',
    desc: 'Merchant is ineligible due to history of financial crimes, fraud, or at risk of DA by Credit Risk.',
    checklist: ['Review blacklist reason', 'This is typically a permanent decline'],
    aan: {name: 'Adverse Account Activity', days: 'Permanent'}
  },
  business_type_blacklisted: {
    title: 'Business Type Blacklisted',
    severity: 'critical',
    sop: '§6.1 / §7.8',
    desc: 'Business type is charitable_orgs, charitable_social_service_organizations, or political_organizations.',
    checklist: ['Verify business type on account', 'Confirm against unsupported industries list'],
    aan: {name: 'Charity Political Org', days: 'Permanent'}
  },
  risk_score_limit_exceeded: {
    title: 'Risk Score Limit Exceeded',
    severity: 'warning',
    sop: '§7.4',
    desc: 'The model has determined elevated risks to repayment.',
    checklist: ['Conduct holistic review of seller', 'Review processing history, chargebacks, GPV trends'],
    aan: {name: 'Uncharacteristic Payment Activity', days: '90 days'}
  },
  in_payment_amount_slump: {
    title: 'Payment Amount Slump',
    severity: 'warning',
    sop: '§7.4',
    desc: 'Card sales in past 4 weeks is less than half of average.',
    checklist: ['Review GPV trends for recent decline', 'Check for seasonal patterns', 'SSP for payment slowdown if non-seasonal gap > 7 days'],
    aan: {name: 'Payment History', days: '90 days'}
  },
  in_payment_count_slump: {
    title: 'Payment Count Slump',
    severity: 'warning',
    sop: '§7.4',
    desc: 'Payment count in past 4 weeks is less than half of average.',
    checklist: ['Review transaction count trends', 'Check for seasonal patterns'],
    aan: {name: 'Payment History', days: '90 days'}
  },
  has_prepay_or_extra_payment_recently: {
    title: 'Recent Prepayment/Extra Payment',
    severity: 'warning',
    sop: '§7.4',
    desc: 'Has an extra payment or prepayment in the last 7 days.',
    checklist: ['Check if manual payment has been completed', 'If still pending → send to escalation queue', 'If payment unsuccessful → decline 90 days (Failed Debits)'],
    aan: {name: 'Failed Debits', days: '90 days'}
  },
  compliance_hashtag_violation_eligibility: {
    title: 'Compliance Hashtag Violation',
    severity: 'warning',
    sop: '§7.7',
    desc: 'Merchant has been tagged with certain hashtags making them ineligible.',
    checklist: ['Review recent comments and hashtags on account', 'Evaluate if seller faces elevated risk of deactivation'],
    aan: null
  },
  not_active_in_last_day: {
    title: 'Not Active in Last Day',
    severity: 'warning',
    sop: '§7.4',
    desc: 'The merchant has no activity in the past 7 days.',
    checklist: ['Check processing history for gaps', 'If non-seasonal gap > 7 days → SSP for payment slowdown'],
    aan: {name: 'Payment History', days: '90 days'}
  }
};

// Generic fallback for unknown blockers
function getBlockerInfo(key) {
  if (CROP_BLOCKERS[key]) return CROP_BLOCKERS[key];
  return {
    title: key.replace(/_/g, ' ').replace(/\b\w/g, c => c.toUpperCase()),
    severity: 'warning',
    sop: 'See SOP',
    desc: 'Auto-risk blocker: ' + key,
    checklist: ['Review account for this signal', 'Determine if it poses risk to repayment', 'Decision per SOP guidance'],
    aan: null
  };
}

function fmt$(cents) {
  return '$' + (cents / 100).toLocaleString('en-US', {minimumFractionDigits: 0, maximumFractionDigits: 0});
}

function fmtDate(d) {
  if (!d) return '—';
  return new Date(d).toLocaleDateString('en-US', {month: 'short', day: 'numeric', year: 'numeric'});
}

function showToast(msg) {
  const t = document.getElementById('toast');
  t.innerHTML = '<i class="fas fa-info-circle"></i> ' + msg;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), 3000);
}

async function lookupApplication() {
  const token = document.getElementById('tokenInput').value.trim();
  if (!token) { showToast('Please enter an application token'); return; }

  document.getElementById('emptyState').style.display = 'none';
  document.getElementById('errorState').style.display = 'none';
  document.getElementById('appContent').style.display = 'none';
  document.getElementById('loadingState').style.display = 'flex';
  document.getElementById('sidebarInfo').style.display = 'none';

  try {
    const res = await fetch(API_BASE + '/application?token=' + encodeURIComponent(token));
    const data = await res.json();

    if (!res.ok || data.error) {
      document.getElementById('loadingState').style.display = 'none';
      document.getElementById('errorState').style.display = 'block';
      document.getElementById('errorTitle').textContent = 'Error';
      document.getElementById('errorMsg').textContent = data.error || 'Unknown error';
      return;
    }

    document.getElementById('loadingState').style.display = 'none';
    currentAppData = data;
    renderDashboard(data);
  } catch (e) {
    document.getElementById('loadingState').style.display = 'none';
    document.getElementById('errorState').style.display = 'block';
    document.getElementById('errorTitle').textContent = 'Connection Error';
    document.getElementById('errorMsg').textContent = 'Cannot reach the server. Make sure server.py is running on localhost:5050. Error: ' + e.message;
  }
}

function renderDashboard(data) {
  const app = data.application[0];
  const m = data.merchant || {};
  const loans = data.loans || [];
  const gpv = data.gpv || [];
  const disputes = data.disputes || [];
  const linked = data.linked_accounts || [];
  const repayment = data.repayment || [];

  // Parse blockers from ALL application steps (not just the first one)
  const allBlockers = new Set();
  data.application.forEach(step => {
    const afr = step.AGGREGATED_AUTO_STEP_FAILURE_REASON || '';
    const fr = step.AUTO_STEP_FAILURE_REASON || '';
    if (afr) afr.split(',').map(b => b.trim()).filter(b => b).forEach(b => allBlockers.add(b));
    if (fr) fr.split(',').map(b => b.trim()).filter(b => b).forEach(b => allBlockers.add(b));
  });
  const blockers = Array.from(allBlockers);

  // Compute signals
  const totalGPV = gpv.reduce((s, r) => s + (r.GPV_PAYMENT_AMOUNT_BASE_UNIT || 0), 0);
  const totalDisputed = disputes.reduce((s, r) => s + (r.DISPUTED_AMOUNT || 0), 0);
  const cbRate = totalGPV > 0 ? (totalDisputed / (totalGPV / 100) * 100) : 0;
  const totalCP = gpv.reduce((s, r) => s + (r.CP_CARD_PAYMENT_COUNT || 0), 0);
  const totalCards = gpv.reduce((s, r) => s + (r.GPV_PAYMENT_COUNT || 0), 0);
  const cpPct = totalCards > 0 ? (totalCP / totalCards * 100) : 0;
  const activeLoans = loans.filter(l => ['allocated', 'originated', 'approved'].includes(l.STATUS));
  const priorCount = app.PRIOR_PLAN_GROUP_COUNT || (loans.length > 0 ? loans[0].PRIOR_PLAN_GROUP_COUNT : 0);
  const offerAmt = app.SELECTED_OFFER_AMOUNT || 0;
  const receivables = app.SELECTED_OFFER_RECEIVABLE_AMOUNT || 0;
  const submitted = app.APPLICATION_SUBMITTED_AT;
  const daysInReview = submitted ? Math.floor((Date.now() - new Date(submitted)) / 86400000) : 0;
  const loanTier = offerAmt > 250000 ? '>$250K' : offerAmt > 75000 ? '$75K-$250K' : offerAmt > 10000 ? '$10K-$75K' : '≤$10K';
  const googleUrl = 'https://www.google.com/search?q=' + encodeURIComponent((m.BUSINESS_NAME || '') + ' ' + (m.RECEIPT_CITY || '') + ' ' + (m.RECEIPT_STATE || ''));
  const regulatorUrl = 'https://regulator.sqprod.co/n/users/' + (app.UNIT_TOKEN || '');

  // Sidebar
  document.getElementById('sidebarInfo').style.display = 'block';
  document.getElementById('merchantInfoCard').innerHTML = `
    <div class="info-row"><span class="label">Business</span><span class="value">${m.BUSINESS_NAME || '—'}
      <a href="${googleUrl}" target="_blank" class="biz-link" title="Google Search"><i class="fas fa-search"></i></a>
      <a href="${regulatorUrl}" target="_blank" class="biz-link" title="Regulator"><i class="fas fa-external-link-alt"></i></a>
    </span></div>
    <div class="info-row"><span class="label">Unit Token</span><span class="value" style="font-family:monospace;font-size:10px">${app.UNIT_TOKEN || '—'}</span></div>
    <div class="info-row"><span class="label">Risk Grade</span><span class="value" style="color:#4ecdc4">${app.RISK_GRADE_NAME || '—'}</span></div>
    <div class="info-row"><span class="label">Location</span><span class="value">${m.RECEIPT_CITY || ''}, ${m.RECEIPT_STATE || ''}</span></div>
    <div class="info-row"><span class="label">MCC</span><span class="value">${(m.BUSINESS_TYPE || '—').replace(/_/g, ' ')}</span></div>
    <div class="info-row"><span class="label">Status</span><span class="value">${m.UNIT_ACTIVE_STATUS ? '✅ Active' : '❌ Inactive'}</span></div>
    <div class="info-row"><span class="label">Frozen</span><span class="value">${m.IS_CURRENTLY_FROZEN ? '🔴 Yes' : '🟢 No'}</span></div>
  `;

  const slaPct = Math.min((daysInReview / 5) * 100, 100);
  const slaColor = slaPct > 80 ? 'var(--red)' : slaPct > 60 ? 'var(--orange)' : 'var(--blue)';
  document.getElementById('slaCard').innerHTML = `
    <div class="info-row"><span class="label">Days in Review</span><span class="value">${daysInReview}</span></div>
    <div class="info-row"><span class="label">Submitted</span><span class="value">${fmtDate(submitted)}</span></div>
    <div class="sla-bar"><div class="sla-bar-fill" style="width:${slaPct}%;background:${slaColor}"></div></div>
    <div class="sla-text"><span>Internal SLA: 2 business days</span><span>External: 3 days</span></div>
  `;

  // Main content
  let html = '';

  // Overview cards
  html += `<div class="section"><div class="section-title">📋 Application Overview</div><div class="overview-cards">
    <div class="ov-card"><div class="ov-label">Loan Amount</div><div class="ov-value">${fmt$(offerAmt * 100)}</div><div class="ov-sub">Tier: ${loanTier}</div></div>
    <div class="ov-card"><div class="ov-label">Receivables</div><div class="ov-value">${fmt$(receivables * 100)}</div></div>
    <div class="ov-card"><div class="ov-label">Risk Grade</div><div class="ov-value"><span class="badge badge-green">${app.RISK_GRADE_NAME || '—'}</span></div></div>
    <div class="ov-card"><div class="ov-label">Product</div><div class="ov-value" style="font-size:13px">${app.PRODUCT_NAME || '—'}</div></div>
    <div class="ov-card"><div class="ov-label">Offer Type</div><div class="ov-value" style="font-size:13px">${app.OFFER_TYPE || '—'}</div></div>
    <div class="ov-card"><div class="ov-label">Days in Review</div><div class="ov-value" style="color:${slaColor}">${daysInReview}</div><div class="ov-sub">SLA: 2-3 business days</div></div>
    <div class="ov-card"><div class="ov-label">SSP Sent</div><div class="ov-value">${app.CASE_HAS_SSP === 'Y' ? '✅ Yes' : '❌ No'}</div></div>
    <div class="ov-card"><div class="ov-label">Prior Loans</div><div class="ov-value">${loans.length}</div></div>
  </div></div>`;

  // Blockers section
  if (blockers.length > 0) {
    html += `<div class="section"><div class="section-title">🚨 Active Blockers (${blockers.length})</div>`;
    blockers.forEach(bk => {
      const info = getBlockerInfo(bk);
      const borderClass = info.severity === 'critical' ? 'border-red' : 'border-orange';
      const tagClass = info.severity === 'critical' ? 'tag-critical' : 'tag-warning';
      const tagText = info.severity === 'critical' ? 'CRITICAL' : 'WARNING';

      html += `<div class="blocker-card"><div class="blocker-inner">
        <div class="blocker-border ${borderClass}"></div>
        <div class="blocker-content">
          <div class="blocker-header"><h3>${info.title}</h3><span class="blocker-tag ${tagClass}">${tagText}</span></div>
          <div class="blocker-code">${bk}</div>
          <div class="blocker-desc">${info.desc}</div>
          <div style="font-size:11px;color:var(--blue);margin-bottom:10px"><i class="fas fa-book"></i> SOP Reference: ${info.sop}</div>
          <ul class="checklist">
            ${info.checklist.map(item => `<li><input type="checkbox" onchange="updateProgress()"><span>${item}</span></li>`).join('')}
          </ul>
          ${info.aan ? `<div class="decline-info"><strong>If decline required:</strong>AAN: ${info.aan.name} — ${info.aan.days}</div>` : ''}
        </div>
      </div></div>`;
    });
    html += `</div>`;
  }

  // Auto-detected signals
  html += `<div class="section"><div class="section-title">📊 Auto-Detected Signals</div>`;

  // Chargeback signal
  const cbIcon = cbRate > 3 ? 'warn' : 'ok';
  html += `<div class="signal-card">
    <div class="signal-header"><div class="signal-icon ${cbIcon}"><i class="fas fa-exclamation-triangle"></i></div><h4>Chargeback Analysis</h4></div>
    <div class="signal-detail"><strong>Disputes (12mo):</strong> ${disputes.length} disputes, $${totalDisputed.toLocaleString()} total disputed</div>
    <div class="signal-detail"><strong>CB Rate:</strong> ${cbRate.toFixed(2)}% of GPV</div>
    <div class="signal-assessment ${cbRate > 3 ? 'assessment-warn' : 'assessment-ok'}">${cbRate > 3 ? '⚠️ CB rate exceeds 3% threshold — §7.4: Decline 180 days (Chargeback History AAN)' : '✅ Chargeback rate within acceptable range'}</div>
  </div>`;

  // Active loan signal
  if (activeLoans.length > 0) {
    html += `<div class="signal-card">
      <div class="signal-header"><div class="signal-icon info"><i class="fas fa-file-invoice-dollar"></i></div><h4>Active Loans</h4></div>
      ${activeLoans.map(l => `<div class="signal-detail"><strong>${l.PRODUCT_NAME}:</strong> ${fmt$(l.FINANCED_CENTS)} — ${l.STATUS} — Risk: ${l.RISK_GRADE_NAME}</div>`).join('')}
      <div class="signal-assessment assessment-ok">ℹ️ Existing loan(s) found. Check repayment status and evasion signals.</div>
    </div>`;
  }

  // CP% signal
  html += `<div class="signal-card">
    <div class="signal-header"><div class="signal-icon ${cpPct < 50 ? 'warn' : 'ok'}"><i class="fas fa-credit-card"></i></div><h4>Card Present %</h4></div>
    <div class="signal-detail"><strong>CP Rate:</strong> ${cpPct.toFixed(1)}%</div>
    <div class="signal-assessment ${cpPct < 50 ? 'assessment-warn' : 'assessment-ok'}">${cpPct < 50 ? '⚠️ CP below 50% — may indicate presale or fraud risk (§7.5)' : '✅ Healthy card-present ratio'}</div>
  </div>`;

  // Linked accounts signal
  if (linked.length > 0) {
    html += `<div class="signal-card">
      <div class="signal-header"><div class="signal-icon warn"><i class="fas fa-link"></i></div><h4>Linked Accounts</h4></div>
      ${linked.map(l => `<div class="signal-detail"><strong>${l.LINKED_UNIT_TOKEN}:</strong> ${l.PRODUCT_NAME} — ${fmt$(l.FINANCED_CENTS)} — ${l.STATUS}</div>`).join('')}
      <div class="signal-assessment assessment-warn">⚠️ Linked accounts with active loans found. Review for evasion (§7.3.1).</div>
    </div>`;
  }

  html += `</div>`;

  // GPV Chart section
  if (gpv.length > 0) {
    const totalGPV30 = gpv.slice(0, 30).reduce((s, r) => s + (r.GPV_PAYMENT_AMOUNT_BASE_UNIT || 0), 0);
    const avgDaily = gpv.length > 0 ? totalGPV / gpv.length / 100 : 0;
    const activeDays = gpv.filter(r => (r.GPV_PAYMENT_COUNT || 0) > 0).length;
    html += `<div class="section"><div class="section-title">📈 GPV Trends (180 Days)</div>
    <div style="background:var(--white);border-radius:var(--radius);box-shadow:var(--shadow);border:1px solid var(--gray-200);padding:16px">
      <div style="display:flex;gap:20px;margin-bottom:12px;font-size:12px">
        <div><strong>Total GPV:</strong> $${(totalGPV / 100).toLocaleString()}</div>
        <div><strong>Last 30 Days:</strong> $${(totalGPV30 / 100).toLocaleString()}</div>
        <div><strong>Avg Daily:</strong> $${Math.round(avgDaily).toLocaleString()}</div>
        <div><strong>Active Days:</strong> ${activeDays} of ${gpv.length}</div>
        <div><strong>CP %:</strong> ${cpPct.toFixed(1)}%</div>
      </div>
      <div id="gpvChartContainer"></div>
    </div></div>`;
  }

  // Smart SSP Suggestions
  const sspSuggestions = getSmartSSPSuggestions(blockers, {loanAmount: offerAmt, cbRate: cbRate});
  if (sspSuggestions.length > 0) {
    html += `<div class="section"><div class="section-title">💡 Suggested SSP Templates</div>
    <div style="background:var(--white);border-radius:var(--radius);box-shadow:var(--shadow);border:1px solid var(--gray-200);padding:16px">
      <p style="font-size:11px;color:var(--gray-500);margin-bottom:10px">Based on the detected blockers and account signals, these SSP templates may be relevant:</p>
      ${sspSuggestions.map(s => `
        <div style="display:flex;align-items:center;gap:10px;padding:8px 0;border-bottom:1px solid var(--gray-100);font-size:12px">
          <div style="width:28px;height:28px;border-radius:50%;background:var(--blue-light);color:var(--blue);display:flex;align-items:center;justify-content:center;flex-shrink:0"><i class="fas fa-envelope"></i></div>
          <div style="flex:1">
            <div style="font-weight:600">${s.name} <span class="badge badge-blue" style="margin-left:4px">${s.cat}</span></div>
            <div style="font-size:11px;color:var(--gray-500)">${s.reason}</div>
          </div>
          <button onclick="openSSPModalWithTemplate('${s.cat}','${s.name}')" style="padding:4px 10px;border:1px solid var(--blue);background:var(--white);color:var(--blue);border-radius:var(--radius-sm);font-size:11px;font-weight:600;cursor:pointer;font-family:inherit">Use</button>
        </div>
      `).join('')}
    </div></div>`;
  }

  // Passed checks
  const passedSteps = data.application.filter(s => s.AUTO_STEP_RESULT === 'approved');
  if (passedSteps.length > 0) {
    html += `<div class="section"><div class="section-title">✅ Passed Checks</div>
    <div class="passed-section">
      <div class="collapsible-header" onclick="this.classList.toggle('open');this.nextElementSibling.classList.toggle('open')">
        <h3><i class="fas fa-check-circle" style="color:var(--green)"></i> ${passedSteps.length} Auto-Approved Steps</h3>
        <i class="fas fa-chevron-down chevron"></i>
      </div>
      <div class="collapsible-body" style="padding:0 18px 14px">
        ${passedSteps.map(s => `<div class="passed-item"><i class="fas fa-check-circle"></i><span>${s.AUTO_STEP_TYPE || s.MANUAL_STEP_TYPE || '—'}</span><span class="pass-value" style="margin-left:auto">Approved</span></div>`).join('')}
      </div>
    </div></div>`;
  }

  // Decision summary
  const totalChecks = blockers.reduce((s, b) => s + getBlockerInfo(b).checklist.length, 0);
  html += `<div class="section"><div class="section-title">📋 Decision Summary</div>
  <div class="decision-section">
    <div style="display:flex;justify-content:space-between;font-size:11px;color:var(--gray-600);margin-bottom:6px">
      <span id="progressText">0 of ${totalChecks} items reviewed</span><span id="progressPct">0%</span>
    </div>
    <div class="progress-bar"><div class="progress-fill" id="progressFill" style="width:0%"></div></div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:16px 0;font-size:12px;color:var(--gray-600)">
      <div><strong>Loan Tier:</strong> ${loanTier}</div>
      <div><strong>Blockers:</strong> ${blockers.length}</div>
      <div><strong>Risk Grade:</strong> ${app.RISK_GRADE_NAME || '—'}</div>
      <div><strong>CB Rate:</strong> ${cbRate.toFixed(2)}%</div>
    </div>
    <div class="decision-buttons">
      <button class="decision-btn d-approve" onclick="showToast('Approve flow coming soon')"><i class="fas fa-check"></i> Approve</button>
      <button class="decision-btn d-decline" onclick="showToast('Decline flow coming soon')"><i class="fas fa-times"></i> Decline</button>
      <button class="decision-btn d-ssp" onclick="openSSPModal()"><i class="fas fa-envelope"></i> Send SSP</button>
      <button class="decision-btn d-escalate" onclick="openEscModal()"><i class="fas fa-arrow-up"></i> Escalate</button>
    </div>
  </div></div>`;

  document.getElementById('appContent').innerHTML = html;
  document.getElementById('appContent').style.display = 'block';
  updateProgress();
  
  // Render GPV chart after DOM is ready
  if (gpv.length > 0) {
    setTimeout(() => renderGPVChart(gpv, 'gpvChartContainer'), 100);
  }
}

function updateProgress() {
  const checks = document.querySelectorAll('#appContent .checklist input[type="checkbox"]');
  const total = checks.length;
  const done = Array.from(checks).filter(c => c.checked).length;
  const pct = total > 0 ? Math.round(done / total * 100) : 0;
  const pt = document.getElementById('progressText');
  const pp = document.getElementById('progressPct');
  const pf = document.getElementById('progressFill');
  if (pt) pt.textContent = done + ' of ' + total + ' items reviewed';
  if (pp) pp.textContent = pct + '%';
  if (pf) pf.style.width = pct + '%';
}

// ============ ESCALATION CHANNELS ============
const ESC_CHANNELS = [
  {name:'ATO Team', channel:'#sfs-ato', use:'Suspected fraud/ATO cases. Do NOT add #ATO to Regulator.', icon:'fas fa-user-shield', color:'var(--red)'},
  {name:'Compliance', channel:'#compliance', use:'TOSV investigations, cash advancing, prepaid abuse.', icon:'fas fa-balance-scale', color:'var(--blue)'},
  {name:'Credit Risk', channel:'#cap-credit-risk-esc', use:'Presale risk, chargebacks, failed ACH debits, negative balance recovery.', icon:'fas fa-chart-line', color:'var(--orange)'},
  {name:'Customer Success', channel:'#sfs-ssp-esc', use:'Cases requiring CS agent follow up or SSP escalations.', icon:'fas fa-headset', color:'var(--green)'},
  {name:'Fraud Ops', channel:'#risk-ops-escalations', use:'Fraud operations escalations.', icon:'fas fa-exclamation-triangle', color:'var(--red)'},
  {name:'Loan App Review', channel:'#sfs-loan-app-review', use:'Credit review questions from new reviewers.', icon:'fas fa-question-circle', color:'var(--blue)'},
  {name:'Recovery', channel:'#recovery', use:'Hard linked accounts with negative balance.', icon:'fas fa-dollar-sign', color:'var(--orange)'},
  {name:'CBD', channel:'#cbd', use:'Sellers who sell CBD but are not fully enrolled.', icon:'fas fa-leaf', color:'var(--green)'},
  {name:'Civil Process', channel:'#civil_process', use:'Sellers frozen due to outstanding lien or linked frozen account with lien.', icon:'fas fa-gavel', color:'var(--gray-700)'},
  {name:'Pre-Origination Lead', channel:'Keely Villa', use:'Any escalation related to the Credit Review SOP.', icon:'fas fa-user-tie', color:'var(--navy)'}
];

let selectedEscChannel = null;
let currentAppData = null;

function openEscModal() {
  document.getElementById('escModal').style.display = 'flex';
  const list = document.getElementById('escChannelList');
  list.innerHTML = ESC_CHANNELS.map((ch, i) => `
    <div onclick="selectEscChannel(${i})" style="padding:10px 14px;background:var(--gray-50);border:1px solid var(--gray-200);border-radius:var(--radius-sm);cursor:pointer;font-size:12px;display:flex;align-items:center;gap:10px;transition:all .15s" id="escCh_${i}" onmouseover="this.style.background='var(--blue-light)'" onmouseout="if(selectedEscChannel!==${i})this.style.background='var(--gray-50)'">
      <i class="${ch.icon}" style="color:${ch.color};width:20px;text-align:center"></i>
      <div style="flex:1">
        <div style="font-weight:600">${ch.name}</div>
        <div style="font-size:10px;color:var(--gray-500)">${ch.use}</div>
      </div>
      <code style="font-size:11px;color:var(--blue);background:var(--blue-light);padding:2px 6px;border-radius:3px">${ch.channel}</code>
    </div>
  `).join('');
  selectedEscChannel = null;
  document.getElementById('escPreview').style.display = 'none';
}

function closeEscModal() {
  document.getElementById('escModal').style.display = 'none';
}

function selectEscChannel(idx) {
  selectedEscChannel = idx;
  // Highlight
  ESC_CHANNELS.forEach((_, i) => {
    const el = document.getElementById('escCh_' + i);
    if (el) {
      el.style.background = i === idx ? 'var(--blue-light)' : 'var(--gray-50)';
      el.style.borderColor = i === idx ? 'var(--blue)' : 'var(--gray-200)';
    }
  });
  // Generate message
  const ch = ESC_CHANNELS[idx];
  const app = currentAppData;
  let msg = `Escalation to ${ch.channel}\n`;
  msg += `─────────────────────────\n`;
  if (app) {
    const a = app.application[0];
    const m = app.merchant || {};
    msg += `Unit Token: ${a.UNIT_TOKEN || '—'}\n`;
    msg += `Business: ${m.BUSINESS_NAME || '—'} (${m.RECEIPT_CITY || ''}, ${m.RECEIPT_STATE || ''})\n`;
    msg += `Application: ${a.APPLICATION_TOKEN || '—'}\n`;
    msg += `Product: ${a.PRODUCT_NAME || '—'} — $${((a.SELECTED_OFFER_AMOUNT || 0)).toLocaleString()}\n`;
    msg += `Risk Grade: ${a.RISK_GRADE_NAME || '—'}\n`;
    const blockerStr = a.AGGREGATED_AUTO_STEP_FAILURE_REASON || a.AUTO_STEP_FAILURE_REASON || '';
    if (blockerStr) msg += `Blockers: ${blockerStr}\n`;
    msg += `─────────────────────────\n`;
    msg += `Reason for escalation: `;
  }
  document.getElementById('escMessage').textContent = msg;
  document.getElementById('escPreview').style.display = 'block';
}

function copyEscMessage() {
  const msg = document.getElementById('escMessage').textContent;
  const notes = document.getElementById('escNotes').value;
  const full = notes ? msg + notes : msg;
  navigator.clipboard.writeText(full).then(() => showToast('Escalation message copied!')).catch(() => showToast('Failed to copy'));
}

function sendEscalation() {
  if (selectedEscChannel === null) { showToast('Please select a team to escalate to'); return; }
  const ch = ESC_CHANNELS[selectedEscChannel];
  showToast('Escalation to ' + ch.channel + ' ready — paste in Slack');
  closeEscModal();
}

// ============ GPV CHART ============
function renderGPVChart(gpvData, containerId) {
  if (!gpvData || gpvData.length === 0) return;
  
  const sorted = [...gpvData].sort((a, b) => new Date(a.THE_DATE) - new Date(b.THE_DATE));
  const values = sorted.map(r => (r.GPV_PAYMENT_AMOUNT_BASE_UNIT || 0) / 100);
  const dates = sorted.map(r => r.THE_DATE);
  
  const canvas = document.createElement('canvas');
  canvas.width = 800;
  canvas.height = 200;
  canvas.style.width = '100%';
  canvas.style.height = '200px';
  const container = document.getElementById(containerId);
  if (!container) return;
  container.appendChild(canvas);
  
  const ctx = canvas.getContext('2d');
  const W = canvas.width, H = canvas.height;
  const pad = {top: 20, right: 20, bottom: 30, left: 60};
  const cW = W - pad.left - pad.right;
  const cH = H - pad.top - pad.bottom;
  
  const maxVal = Math.max(...values) * 1.1 || 100;
  const minVal = 0;
  
  // Background
  ctx.fillStyle = '#fff';
  ctx.fillRect(0, 0, W, H);
  
  // Grid lines
  ctx.strokeStyle = '#f1f3f5';
  ctx.lineWidth = 1;
  for (let i = 0; i <= 4; i++) {
    const y = pad.top + (cH / 4) * i;
    ctx.beginPath();
    ctx.moveTo(pad.left, y);
    ctx.lineTo(W - pad.right, y);
    ctx.stroke();
    // Y labels
    const val = maxVal - (maxVal / 4) * i;
    ctx.fillStyle = '#868e96';
    ctx.font = '10px Inter, sans-serif';
    ctx.textAlign = 'right';
    ctx.fillText('$' + Math.round(val).toLocaleString(), pad.left - 8, y + 4);
  }
  
  // Compute 7-day rolling average
  const rolling = [];
  for (let i = 0; i < values.length; i++) {
    const start = Math.max(0, i - 6);
    const slice = values.slice(start, i + 1);
    rolling.push(slice.reduce((a, b) => a + b, 0) / slice.length);
  }
  
  // Draw bars
  const barW = Math.max(1, (cW / values.length) - 1);
  values.forEach((v, i) => {
    const x = pad.left + (i / values.length) * cW;
    const h = (v / maxVal) * cH;
    const y = pad.top + cH - h;
    
    // Detect anomalies (>2x rolling avg)
    const isSpike = rolling[i] > 0 && v > rolling[i] * 2;
    const isDrop = rolling[i] > 0 && v < rolling[i] * 0.3 && v > 0;
    
    ctx.fillStyle = isSpike ? '#f39c12' : isDrop ? '#e74c3c' : '#2d7ff9';
    ctx.globalAlpha = 0.7;
    ctx.fillRect(x, y, barW, h);
    ctx.globalAlpha = 1;
  });
  
  // Draw rolling average line
  ctx.strokeStyle = '#e74c3c';
  ctx.lineWidth = 2;
  ctx.beginPath();
  rolling.forEach((v, i) => {
    const x = pad.left + (i / values.length) * cW + barW / 2;
    const y = pad.top + cH - (v / maxVal) * cH;
    if (i === 0) ctx.moveTo(x, y);
    else ctx.lineTo(x, y);
  });
  ctx.stroke();
  
  // X-axis labels (show ~6 dates)
  ctx.fillStyle = '#868e96';
  ctx.font = '10px Inter, sans-serif';
  ctx.textAlign = 'center';
  const step = Math.floor(dates.length / 6);
  for (let i = 0; i < dates.length; i += step) {
    const x = pad.left + (i / values.length) * cW;
    const d = new Date(dates[i]);
    ctx.fillText(d.toLocaleDateString('en-US', {month: 'short', day: 'numeric'}), x, H - 8);
  }
  
  // Legend
  ctx.fillStyle = '#2d7ff9';
  ctx.fillRect(pad.left, H - 18, 10, 10);
  ctx.fillStyle = '#495057';
  ctx.font = '10px Inter, sans-serif';
  ctx.textAlign = 'left';
  ctx.fillText('Daily GPV', pad.left + 14, H - 9);
  
  ctx.strokeStyle = '#e74c3c';
  ctx.lineWidth = 2;
  ctx.beginPath();
  ctx.moveTo(pad.left + 90, H - 13);
  ctx.lineTo(pad.left + 105, H - 13);
  ctx.stroke();
  ctx.fillStyle = '#495057';
  ctx.fillText('7-day Avg', pad.left + 109, H - 9);
  
  ctx.fillStyle = '#f39c12';
  ctx.fillRect(pad.left + 180, H - 18, 10, 10);
  ctx.fillStyle = '#495057';
  ctx.fillText('Spike', pad.left + 194, H - 9);
  
  ctx.fillStyle = '#e74c3c';
  ctx.fillRect(pad.left + 230, H - 18, 10, 10);
  ctx.fillStyle = '#495057';
  ctx.fillText('Drop', pad.left + 244, H - 9);
}

// ============ SMART SSP SUGGESTIONS ============
function getSmartSSPSuggestions(blockers, signals) {
  const suggestions = [];
  
  blockers.forEach(b => {
    switch(b) {
      case 'has_recent_sketchy_login':
      case 'customer_has_recent_email_change':
        // ATO-related — no SSP, escalate to ATO
        break;
      case 'plan_amount_too_large':
        if (signals.loanAmount > 75000) {
          suggestions.push({cat:'Debt & Liens', name:'UCC Lien (DOC)', reason:'Loan >$75K requires UCC lien search. If liens found, request documentation.'});
          suggestions.push({cat:'Business Legitimacy', name:'Business Government Documentation (DOC)', reason:'Large loan — verify business legitimacy.'});
        }
        break;
      case 'chargeback_amount_too_large_last_year':
      case 'chargeback_amount_too_large_last_twelve_weeks':
      case 'chargeback_count_too_large_last_four_weeks':
        suggestions.push({cat:'Processing', name:'Chargeback Inquiry (TEXT)', reason:'Chargeback blocker detected — understand reason for increase.'});
        break;
      case 'frozen_recently':
      case 'frozen':
        // Escalate, not SSP
        break;
      case 'in_payment_amount_slump':
      case 'in_payment_count_slump':
      case 'in_active_days_slump':
        suggestions.push({cat:'Processing', name:'Payment Slowdown (TEXT)', reason:'Processing slump detected — understand reason for decline.'});
        break;
      case 'not_active_in_last_day':
      case 'trailing_four_weeks_active_days_less_than_minimum':
        suggestions.push({cat:'Processing', name:'Processing Gap (TEXT)', reason:'Recent inactivity — understand processing gap.'});
        break;
      case 'customer_has_recent_bank_account_change':
        suggestions.push({cat:'Business Legitimacy', name:'Recent Bank Account Change (DOC)', reason:'Recent bank change — verify legitimacy to avoid ATO risk.'});
        break;
      case 'connected_users_have_open_plan':
      case 'connected_users_deactivated_frozen':
        suggestions.push({cat:'Processing', name:'Same Business Inquiry (TEXT)', reason:'Linked account signal — determine if same business / evasion.'});
        suggestions.push({cat:'Processing', name:'Linked Account Slow Down (TEXT)', reason:'Check if linked account processing has declined.'});
        break;
      case 'not_activated':
      case 'account_deleted':
        suggestions.push({cat:'Individual Verification', name:'Deleted Account (TEXT)', reason:'Account inactive/deleted — verify merchant intent.'});
        break;
      case 'has_prepay_or_extra_payment_recently':
        // Wait for payment to complete, no SSP needed initially
        break;
      case 'risk_score_limit_exceeded':
        suggestions.push({cat:'Processing', name:'Payment Volume Change (TEXT)', reason:'Risk score exceeded — understand recent processing changes.'});
        break;
      default:
        break;
    }
  });
  
  // Signal-based suggestions
  if (signals.cbRate > 2) {
    const exists = suggestions.find(s => s.name === 'Chargeback Inquiry (TEXT)');
    if (!exists) suggestions.push({cat:'Processing', name:'Chargeback Inquiry (TEXT)', reason:`CB rate is ${signals.cbRate.toFixed(1)}% — investigate chargeback activity.`});
  }
  if (signals.refundSpike) {
    suggestions.push({cat:'Processing', name:'Refund Inquiry (TEXT)', reason:'Unusual refund activity detected.'});
  }
  
  return suggestions;
}

// ============ SSP FUNCTIONS ============
function openSSPModal() {
  const modal = document.getElementById('sspModal');
  modal.style.display = 'flex';
  // Build category buttons
  const cats = Object.keys(SSP_TEMPLATES);
  document.getElementById('sspCategoryBtns').innerHTML = cats.map(c =>
    `<button onclick="selectSSPCategory('${c}')" style="padding:6px 12px;border:1px solid var(--gray-300);border-radius:var(--radius-sm);font-size:11px;font-weight:600;cursor:pointer;background:var(--white);font-family:inherit;transition:all .15s" id="sspCat_${c.replace(/[^a-zA-Z]/g,'')}">${c}</button>`
  ).join('');
  if (cats.length > 0) selectSSPCategory(cats[0]);
}

function closeSSPModal() {
  document.getElementById('sspModal').style.display = 'none';
}

function selectSSPCategory(cat) {
  currentSSPCategory = cat;
  // Highlight selected category
  document.querySelectorAll('#sspCategoryBtns button').forEach(b => {
    b.style.background = 'var(--white)';
    b.style.color = 'var(--gray-700)';
    b.style.borderColor = 'var(--gray-300)';
  });
  const btn = document.getElementById('sspCat_' + cat.replace(/[^a-zA-Z]/g, ''));
  if (btn) {
    btn.style.background = 'var(--blue)';
    btn.style.color = 'var(--white)';
    btn.style.borderColor = 'var(--blue)';
  }
  // Populate template dropdown
  const templates = SSP_TEMPLATES[cat] || [];
  const sel = document.getElementById('sspTemplateSelect');
  sel.innerHTML = '<option value="">— Select an SSP template —</option>' +
    templates.map((t, i) => `<option value="${i}">${t.name} [${t.type}]</option>`).join('');
  document.getElementById('sspPreview').style.display = 'none';
}

function showSSPTemplate() {
  const sel = document.getElementById('sspTemplateSelect');
  const idx = sel.value;
  if (idx === '' || !currentSSPCategory) {
    document.getElementById('sspPreview').style.display = 'none';
    return;
  }
  const t = SSP_TEMPLATES[currentSSPCategory][parseInt(idx)];
  document.getElementById('sspType').innerHTML = `<span class="badge ${t.type === 'DOC' ? 'badge-orange' : 'badge-blue'}">${t.type}</span> ${t.name}`;
  document.getElementById('sspText').textContent = t.text;
  document.getElementById('sspUse').innerHTML = '<strong>When to use:</strong> ' + t.use;
  document.getElementById('sspPreview').style.display = 'block';
}

function copySSPText() {
  const text = document.getElementById('sspText').textContent;
  const notes = document.getElementById('sspNotes').value;
  const full = notes ? text + '\n\nNotes: ' + notes : text;
  navigator.clipboard.writeText(full).then(() => {
    showToast('SSP text copied to clipboard!');
  }).catch(() => showToast('Failed to copy'));
}

function openSSPModalWithTemplate(cat, name) {
  openSSPModal();
  selectSSPCategory(cat);
  // Find and select the template
  const templates = SSP_TEMPLATES[cat] || [];
  const idx = templates.findIndex(t => t.name === name);
  if (idx >= 0) {
    document.getElementById('sspTemplateSelect').value = idx;
    showSSPTemplate();
  }
}

function sendSSP() {
  const sel = document.getElementById('sspTemplateSelect');
  if (sel.value === '') { showToast('Please select an SSP template first'); return; }
  const t = SSP_TEMPLATES[currentSSPCategory][parseInt(sel.value)];
  showToast('SSP "' + t.name + '" ready to send via Regulator');
  closeSSPModal();
}

// Allow Enter key to search
document.getElementById('tokenInput').addEventListener('keypress', e => {
  if (e.key === 'Enter') lookupApplication();
});
</script>
</body>
</html>
